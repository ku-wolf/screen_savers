# Screen Savers

Uses kutils4pygame to create simple screen savers. Includes Matrix and Line Tracer examples.

# Installation
clone repo
pip3 install -r requirements.txt
./setup.py install

# Usage

matrix (for matrix example)
line_tracer (for line_tracer example)
